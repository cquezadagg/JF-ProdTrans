// import { useAppContext } from "@/hooks/useAppContext";
//
// export async function UpdateFactura(manifestNum: string, estado: string) {
//   const { manifestsData, setManifestsData } = useAppContext();
//   const manifest = await getManifest(manifestNum);
//   manifest.facturas.forEach((factura) => {
//     if (factura.numFactura === manifestNum) {
//       factura.estado = estado;
//     }
//   });
// }
